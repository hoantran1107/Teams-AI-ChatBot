sonar.projectKey=infodation_ifd-cpb-python-ai_a72a641c-5c37-4147-a253-be4621209bc3
sonar.organization=infodation

# This is the name and version displayed in the SonarCloud UI.
#sonar.projectName=ifd-cpb-python-ai
#sonar.projectVersion=1.0

# Coverage configuration
sonar.python.coverage.reportPaths=coverage.xml

# Path is relative to the sonar-project.properties file. Replace "\" by "/" on Windows.
#sonar.sources=.

# Encoding of the source code. Default is default system encoding
#sonar.sourceEncoding=UTF-8
# Ignore SonarCloud issues in Dockerfile
# This rule warns when a Docker image might run as the default 'root' user
# Example warning: "This image might run with 'root' as the default user. Make sure it is safe here."
sonar.issue.ignore.multicriteria=e1
sonar.issue.ignore.multicriteria.e1.ruleKey=docker:S6471
sonar.issue.ignore.multicriteria.e1.resourceKey=**/Dockerfile

# Ignore automatically generated files from exensions
## Teams Toolkit
sonar.exclusions=infra/**,appPackage/**